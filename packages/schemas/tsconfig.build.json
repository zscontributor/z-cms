{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Emit CommonJS: cms-api (NestJS) requires this at runtime, and the Next.js
    // bundlers consume CJS without complaint.
    "module": "CommonJS",
    "moduleResolution": "Node",
    "noEmit": false,
    "declaration": true,
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "src/**/*.test.ts", "src/**/*.test.tsx"]
}
