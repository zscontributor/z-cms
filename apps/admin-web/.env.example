# URL of the NestJS cms-api. The admin talks to it server-side only.
CMS_API_URL="http://localhost:4000"

# Production mounts the admin on each site's origin at /admin, for example:
# https://z-cms.org/admin. Leave empty in local dev to keep http://localhost:3101.
ADMIN_BASE_PATH=""
